# 🎯 在VSCode的ESP-IDF终端中运行这些命令

## 方法1：分步烧录（推荐，100%成功）

# 步骤1: 烧录固件和分区表
idf.py -p /dev/tty.usbmodem101 flash

# 步骤2: 单独烧录model分区（这步很重要！）
esptool.py -p /dev/tty.usbmodem101 write_flash 0x800000 build/srmodels/srmodels.bin

# 步骤3: 监控日志
idf.py -p /dev/tty.usbmodem101 monitor

## 方法2：一行命令（如果esptool可用）

esptool.py -p /dev/tty.usbmodem101 -b 460800 --before default_reset --after hard_reset --chip esp32s3 write_flash --flash_mode dio --flash_size 16MB --flash_freq 80m 0x0 build/bootloader/bootloader.bin 0x8000 build/partition_table/partition-table.bin 0xd000 build/ota_data_initial.bin 0x10000 build/xiaozhi.bin 0x800000 build/srmodels/srmodels.bin 0x900000 build/generated_assets.bin

## 验证

# 查看烧录的内容（读取前32字节）
esptool.py -p /dev/tty.usbmodem101 read_flash 0x800000 32 model_check.bin
hexdump -C model_check.bin

# 应该看到：03 00 00 00（表示3个模型）

## 如果esptool.py不可用

# 使用python3 -m esptool
python3 -m esptool -p /dev/tty.usbmodem101 write_flash 0x800000 build/srmodels/srmodels.bin























